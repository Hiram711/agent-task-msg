# Codex 联调结果

验证日期：2026-09-20。环境：Windows、Windows PowerShell 5.1、Codex CLI 0.154.0，以及桌面版内置核心 0.155.0-alpha.9.2。

## 已通过

- 安装器将运行文件复制到 `$CODEX_HOME/skills/agent-task-msg`，并合并 `$CODEX_HOME/hooks.json`。
- Codex 的 `hooks/list` 能识别 `permissionRequest`，无配置错误或警告；用户审查后状态为 `trusted`。
- 向已注册命令注入中文审批事件，完整走通 `dispatch → wx_notify → wx_send`。
- 发送器 OCR 核对目标为「文件传输助手」，确认输入框有字且按回车后清空，返回成功。消息区 OCR 未读到标题，因此未独立验证手机端收件。
- 13 项离线回归测试通过，包括中文/BOM、空参数、特殊字符路径、去重、安装幂等性及其它 hook 的保留。
- 测试完成后恢复原配置，推送关闭、空闲阈值 120 秒；无测试截图遗留。

## 早期现象：当前桌面任务的权限通知未接通

hook 已受信任、推送临时开启且空闲判断关闭时，当前桌面任务的提权请求和 `request_permissions` 网络权限申请均未产生新通知日志或 payload。重启后复测结果相同。

因此，**手工事件注入成功不能视为桌面端自动审批提醒成功**。官方文档描述的 shell escalation / managed-network approval 不足以证明所有客户端的 `request_permissions` 路径均会触发该事件。

目前尚不能仅凭无日志区分「事件未派发」和「宿主未运行命令」。需要进一步定位宿主执行路径；重复重启不能解决已观察到的问题。

这项早期结果不代表全部桌面核心审批都不支持。当前任务使用 `auto_review`，请求未必需要等待用户；
后续在独立核心进程中完成了人工审批对照，结果见下。

## 桌面访问

在受限 shell 中运行微信诊断时，可能无法访问真实桌面（前台句柄为零、截图失败）；经正常权限审批后在真实桌面运行同一诊断成功，微信窗口和中文 OCR 可用。

整个测试没有改写 hook 信任记录、跳过信任检查或长期打开推送。

## 新增：回合失败和主动提问通知（2026-09-20）

- 完整回归共 23 项通过：原有 13 项、新场景 9 项、真实 Codex 本机集成 1 项。
- 真实集成使用临时 `CODEX_HOME`、桌面版核心 0.155.0-alpha.9.2 和本机 HTTP 500 服务，无外部模型请求、无 API 密钥。先建立历史失败基线，再产生新失败；独立 App Server 读取到 `failed` 后，实际走通 `codex_watch → codex_notify → wx_notify → 替身发送器`。
- 验证历史失败不补发、新失败只处理一次、重启不重报、运行中重试/正常完成/用户中断不提醒、观察器读取错误不冒充模型失败。
- 主动提问实际走通 `codex_notify → wx_notify → 替身发送器`，验证中文标题和摘要、触发器开关、总开关与临时 payload 清理。
- 新场景测试没有操作微信，不能据此宣称真实 API 故障已经送达手机，或所有提问界面都有自动监听。真实微信发送层复用上面的已测链路。
- 新入口与原权限 hook 独立；桌面审批通知仍有上述未接通限制。
- 已安装版本在真实当前桌面任务执行 `check → start → status → stop`：可读取 11 个持久化回合，状态由 `running` 正常转为 `stopped`。推送关闭、空闲阈值 120 秒、原 `hooks.json` 与 `config.json` 内容保持不变。

## 权限入口定位：人工审批对照（2026-09-20 13:14）

新增分阶段 `dispatch.jsonl`，并用 `tools/probe_codex_approvals.py` 保存可复现的对照结果。
使用桌面内置核心 0.155.0-alpha.9.2、已有受信任的同一 hook、推送关闭、只读沙盒，
通过本机模拟模型创建两个临时会话；仅测试会话使用 `on-request` / `user`。
请求均真实挂起两秒后由测试客户端拒绝，没有批准命令或网络访问，没有人工点击桌面审批卡片。

| 路径 | 实际待审批请求 | Hook 生命周期 | 入口日志 |
| --- | --- | --- | --- |
| `exec_command` + `require_escalated` | `item/commandExecution/requestApproval` | `hook/started`、`hook/completed`，状态 completed | 从 entered 到 worker_started 全部出现 |
| `request_permissions` 网络申请 | `item/permissions/requestApproval` | 未出现 | 未出现 |

命令最终状态为 `declined`。这次已排除自动审批抢先完成、未信任、中文解析失败、推送开关和微信发送故障对上述对照的干扰。
可以确认该版本中两条核心审批路径的 hook 行为不同；`request_permissions` 的缺口在通知脚本之前。
尚未确认是设计范围还是实现遗漏，未将其冒称为已修复的桌面缺陷。

现有脚本无法让宿主补发不存在的 Hook。完整修复需让该审批路径派发 `PermissionRequest`，或从承载任务的实时连接接收
`item/permissions/requestApproval` 并转为通知。尚未实现或验证现有桌面连接的事件桥接；持久化错误观察器不能代替它。
预先发送“即将申请权限”可以作为另行选择的补充，但不能冒称为真实待审批通知。本次没有添加这项可能误报的替代行为。

用户随后明确：可自动审批的请求不需要触发微信提醒。后续排查仅关注确实需要用户处理的审批；
不为自动审批补“一申请就发”的入口。自动拒绝后需要向用户提问时，复用主动提问通知。

回归中还定位到一个测试环境问题：临时 App Server 的插件市场刷新启动 Git 子进程，退出后短时占用临时目录。
本机模拟模型测试现已关闭测试进程内的插件发现，避免无关刷新；未改用户插件配置。
最终完整回归 25 项通过，包括新增的解析失败/关闭推送诊断和日志不可写仍能运行的检查。

## 真实微信端到端复测（2026-09-20 13:50）

本次测试编号 `74b8a766`，目标为「文件传输助手」。测试期间临时打开推送并将空闲阈值设为 0，
结束后恢复原配置：推送关闭、空闲阈值 120 秒，测试观察器已停止、测试截图已删除。

| 场景 | 实际触发来源 | 本机发送结果 |
| --- | --- | --- |
| 待命令审批 | 独立桌面核心 App Server 的真实人工审批请求，已受信任 PermissionRequest hook 自动执行 | 13:50:14 成功，随后测试客户端拒绝命令，未执行测试命令 |
| 回合失败 | 本机 HTTP 500 服务令真实 Codex 回合持久化为 failed，独立观察器检测到新失败 | 13:50:32 成功 |
| 等待回答 | 当前任务调用提问通知入口，询问手机端收件情况 | 13:50:49 成功，随后展示问题卡片 |

发送器逐条核对目标标题，确认正文进入输入框及按回车后输入框清空；第三条还通过消息区 OCR 确认。
用户随后确认手机端三条全部收到。审批用的是独立核心测试连接，未冒充当前桌面 UI 审批验收。
`request_permissions` 的原有 Hook 缺口仍存在。本次没有为自动审批增加提醒。

真实发送还发现并修复了两个此前快速替身测试没有覆盖的问题：

1. 后台工作进程继承 Hook 输出管道，实际发送超过 15 秒后被宿主超时终止。
   同一真实核心用 25 秒慢发送器复现：修复前 Hook 在 15,229 ms 失败、发送器只运行 14 秒；
   改用隐藏窗口的 ShellExecute 分离管道后，Hook 在 1,142 ms 正常结束，发送器完整运行 25 秒。
   新增慢发送器回归已通过，确保 Hook 返回不等发送结束。
2. 微信依赖在分栏识别失败时猜测 21% 窗宽，实际 x=188 落在真实分栏 x=308 的左侧，
   引发会话切换和测试草稿残留；校验失败后未按回车，用户已清理草稿。
   本机 `wechat-send-skill` 已取消猜测，新增输入框与标题坐标交叉检查，以及点击后和发送前的目标复核。
   三项离线回归与真实 DryRun 通过后，才执行上述三条发送。该依赖修正另存于其本地源码副本。

## 继续定位 request_permissions（2026-09-20）

公开官方源码中的 `request_permissions_for_environment` 直接调用 Guardian，绕过包含权限 Hook 的公共审批入口；
没有自动审查决定时直接发出人工权限请求。这与本机“有 requestApproval、没有 Hook”的对照一致，
但公开源码版本不能当成本机 alpha 二进制的精确构建来源。

已保存[核心修复提案及补丁](https://github.com/Hiram711/agent-task-msg/tree/main/proposals)，按“自动审查有决定则不提醒，转人工时才运行 Hook”的要求设计。
仅通过源码补丁应用检查，未编译、未替换桌面核心、未完成修复后的实际验收。因此这项缺口仍应标为未解决。
